# 0.1.5 已知缺陷、绕过方式和未覆盖范围

本表只汇总已检查范围，不代表全仓审计。版本仍为自用预览版。状态含义如下：

- “有限降级”：当前工作流可继续，但某项语义明确不等价；
- “绕过”：避开 Clang 无法表达的 ARMCC5 路径，不代表实现了原行为；
- “未实现”：没有可靠处理，不能通过隐藏诊断宣称支持。

## 已有缺陷和未完成项

| ID | 项目 | 当前状态 | 绕过/操作 | 剩余影响 |
| --- | --- | --- | --- | --- |
| AC5-01 | ARMCC5 专有语义 | 未实现 | CMSIS 采用 GCC/Clang 分支；最终看原源码及 ARMCC5 构建 | 汇编、寄存器变量、pragma、调用约定可能误报或漏报 |
| AC5-02 | 打开的 packed 文件 | 有限降级 | 空 `__packed` fallback 保持声明、跳转和补全 | 内存缓冲区不表示 packed 布局；只可用保存文件 VFS/ARMCC5 验证布局 |
| AC5-03 | packed 的全部形式 | 未实现 | 只转换明确的 struct/union 定义 | 指针、已有类型限定和宏展开复杂声明可能仍失败，不能证明完整 ABI |
| AC5-04 | 完整依赖扫描 | 未实现 | 扫描字面量 include | 不执行完整预处理条件；include_next、宏 include 和特殊参数可能漏依赖 |
| AC5-05 | 非 ASCII include 编码 | 部分支持 | UTF-8 路径已测试，快照保持原字节 | 旧编码中的非 ASCII 文件名未保证解析 |
| AC5-06 | 外部头文件更新 | 部分覆盖 | 手动执行 `Refresh Keil Project` | 工作区外或特殊扩展名文件不一定触发自动刷新 |
| AC5-07 | VFS 历史副本回收 | 未实现 | 内容哈希避免原文件覆盖和半更新 | 旧快照长期积累，占用工作区存储 |
| LEGACY-01 | cpptools 参数精度 | 未整改 | 保留上游 Target 级配置 | 不能完整反映逐文件选项、复杂宏及 ARM 数据模型 |
| LEGACY-02 | cpptools 占位宏 | 保留上游行为 | `__packed=`、`__attribute__(x)=` 和常量内建函数维持可编辑性 | 会丢失布局、属性和内建函数语义；红线少不代表准确 |
| CFG-01 | 用户配置冲突 | 提示处理 | 先明确 `compile-commands-dir` 配置所有者，再切换后端 | 插件不强制覆盖用户 `.clangd` 或现有数据库 |
| CFG-02 | 无效 VS Code JSONC | 拒绝覆盖 | 修复对应 `settings.json`/`tasks.json` 后执行配置命令 | 为保留用户内容，语法错误或类型错误时该文件不会自动重写 |
| TOOL-01 | clangd 重构自测失败 | 未修复 | 将重构自测与源码诊断分开报告 | 23.1.0 在 2 个宏表达式上发生 SwapBinaryOperands 替换重叠，完整检查不能记为全过 |
| REPO-01 | 既有 lint warning | 11 个未整改 | 保留报告，不扩大本次范围 | 不影响 TypeScript 编译，但仍是待清理技术债 |
| VAL-01 | 0.1.4 真实编辑器复验 | 未完成 | 重载已安装新版并刷新 Keil 工程 | 代码、测试、打包和安装通过，不等于截图中的 Problems 面板及数据库迁移已复验 |
| VAL-02 | 固件及硬件验收 | 未执行 | 使用实际 UV4 Target 新构建；硬件另行授权测试 | clangd/单元测试不能证明 ARMCC5 链接、下载或设备行为 |
| VAL-03 | 0.1.5 原生任务/UI 复验 | 未执行 | 安装 VSIX、重载窗口，核对工程树按钮与 `Ctrl+Shift+B` 任务 | 单元测试和 VSIX 打包不等于真实 VS Code 交互通过 |

## 正在使用的兼容处理及代价

### 取消 __CC_ARM

目的：避开 Clang 无法解析的 CMSIS ARMCC 汇编分支，使用现有 GCC/Clang 实现。
代价：取消的是编译命令中的宏，影响整个翻译单元，不只影响 CMSIS。
用户业务代码若也依据该宏分支，编辑器观察到的分支与 ARMCC5 可能不同，被跳过代码可能漏报。
该处理不进入 UV4 构建，不能用它证明两个分支功能或时序等价。

### Clang ACLE 和声明宏

使用 Clang 自带 arm_acle.h 及支持的声明属性，避免把指令函数伪造成返回 0。
这仍是 Clang 的内建语义，不覆盖所有 ARMCC5 内建函数、对齐与调用约定。

### __ARM_NO_DEPRECATED_FUNCTIONS=1

使用 ARM 库原有开关排除含 __value_in_regs 的废弃运行时函数声明。
这是避免解析不支持 API 的绕过，不是实现专有寄存器返回 ABI；调用这些函数仍不受支持。
本次参考工程没有发现对这些废弃除法函数的调用。

### packed VFS 副本

对支持的类型定义改变 Clang 读取的文本位置，使用实际 packed 属性，保留字节偏移和
原文件路径。原文件不变；当前只验证了受支持形式和布局样例，不证明所有嵌套类型或 ABI。
内存缓冲区优先于该副本。0.1.3 在这一路径将 `__packed` 展开为空，只恢复声明图；
复杂预处理依赖和类型限定语义仍是限制。不得在打开文件中依据 `sizeof`、字段偏移或
对齐提示判断 ARMCC5 布局。

### 打开缓冲区的空 __packed fallback

仅当工程已检测到需要 packed VFS 映射时，数据库才同时加入 `-D__packed=`。保存文件的
快照已把受支持定义改成实际属性，不依赖该空宏；打开文档草稿绕过快照时才由空宏保持
typedef、引用、跳转和补全可用。

代价是打开缓冲区内的 record 按普通 Clang 布局解释。该路径不能用于布局检查，也不能
复制到 Keil 正式宏定义。若业务代码在条件表达式中依赖 `__packed` 是否为宏，clangd 所见
结果也可能与 ARMCC5 不同。

### 原版 IntelliSense 的空宏与常量

该路径继续保留；与 clangd 的新适配不同。它可能减少红线，却可能影响 sizeof/对齐、
属性检查、常量推导和代码分支判断。切换为 cpptools 不等于获得原生 ARMCC5 分析。

以上兼容参数仅用于编辑器。不要把它们复制到 Keil 的正式宏定义或据此自动改动固件。
